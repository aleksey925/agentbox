# Go environment preset
services:
  agentbox:
    environment:
      - GOPATH=/home/box/go
    volumes:
      - go-cache:/home/box/go

volumes:
  go-cache:
    name: agentbox-go-cache
    external: true
