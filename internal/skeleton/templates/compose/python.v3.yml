# python environment preset
services:
  agentbox:
    environment:
      - UV_LINK_MODE=copy
      - POETRY_VIRTUALENVS_CREATE=false
    volumes:
      - uv-cache:/home/box/.cache/uv

volumes:
  uv-cache:
    name: agentbox-uv-cache
    external: true
